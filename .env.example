# OpenAI key for memgraph embeddings (hybrid recall + embedding-bearing writes).
# Model is fixed: text-embedding-3-small @ 512 dims. Pure-SQL commands
# (session-context, next-wf, ...) work without the key.
OPENAI_API_KEY=sk-...
