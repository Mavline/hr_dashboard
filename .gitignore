# secrets — never commit
.env

# data (PII) — never commit
HR report.xlsx
*.xlsx

# memgraph: per-machine layers, rebuilt on each machine (see memgraph/install/README.md)
memgraph/venv/
memgraph/vendor/

# .agent: track only the canonical memory DB and frozen task specs
.agent/*
!.agent/memory.db
!.agent/tasks/
